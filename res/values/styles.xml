<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Sherlock.Light">
        <item name="android:actionBarStyle">@style/MensaActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MensaActionBar</item>

    </style>

    <style name="MensaActionBar" parent="Widget.Sherlock.Light.ActionBar.Solid">
        <item name="android:background">@color/abs__background_holo_light</item>
        <!-- Support library compatibility -->
        <item name="background">@color/abs__background_holo_light</item>
    </style>

</resources>
